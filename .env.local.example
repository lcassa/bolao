# Geradas automaticamente pelo Vercel ao conectar o KV.
# Copie este arquivo para .env.local e preencha com os valores do painel Vercel > Storage > KV > .env.local
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=
ADMIN_PASSWORD=
